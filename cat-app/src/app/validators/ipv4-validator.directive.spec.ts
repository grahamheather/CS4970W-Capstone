import { Ipv4ValidatorDirective } from './ipv4-validator.directive';

describe('Ipv4ValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Ipv4ValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
